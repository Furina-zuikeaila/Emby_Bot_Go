用户相关命令

/start
- 作用：与机器人建立私聊并进入主界面；支持深链 payload（例如邀请/续费/管理员深链）。
- 用法：私聊机器人直接发送 `/start`，或在链接中附带 payload（例如邀请码或续费码）。
- 权限：任意用户（私聊）。
- 备注：如果 payload 是邀请码会尝试兑换并引导注册；payload 为续费码会尝试兑换续期。

/admin
- 作用：打开管理员控制面板（图形化菜单）。
- 用法：私聊机器人发送 `/admin`。
- 权限：仅管理员（在配置中列出的 Admin IDs）。
- 备注：显示系统统计、注册设置等管理入口。

/users
- 作用：分页查看用户列表（管理员视图）。
- 用法：私聊 `/users [offset]`，offset 为可选的页码偏移。
- 权限：管理员（私聊）。

/user
- 作用：查看某个用户的详细信息（含设备/播放/审计等）。
- 用法：`/user <tg_id>`（私聊）或在管理员界面通过深链跳转（点击用户后使用 `/start` 深链）。
- 权限：管理员（私聊）。

/ucr
- 作用：管理员手动为某个 TGID 创建账号并尝试把账号信息私信给目标用户。
- 用法：`/ucr <tg_id> [emby_username]`（私聊）。
- 权限：管理员（私聊）。
- 备注：若目标未与机器人私聊，私信可能发送失败。

/urm
- 作用：管理员删除（彻底移除）某个用户（危险操作，需要确认）。
- 用法：`/urm <tg_id> --yes`（私聊）。
- 权限：管理员（私聊）。
- 备注：必须带 `--yes` 确认才会执行。

/resetpass
- 作用：管理员为用户重置 Emby 密码并尝试通知用户。
- 用法：`/resetpass <tg_id>`（或回复用户消息并 `/resetpass`）。
- 权限：管理员（私聊）。

/renew
- 作用：管理员为单个用户续期（增加或减少天数，可用正负数）。
- 用法：
  - 私聊：`/renew <tg_id|emby_username> <+/-天数>`
  - 或回复某用户消息：回复后发送 `/renew <+/-天数>`。
- 权限：管理员（私聊）。

/renewall
- 作用：管理员对所有（或符合条件的）用户批量续期。
- 用法：`/renewall <+/-天数>`（私聊）。
- 权限：管理员（私聊）。
- 备注：仅对特定等级的用户生效（代码中说明）。

/Whitelist
/whitelist
- 作用：管理员将 TGID 加入白名单（注册/权限优先级相关）。
- 用法：`/Whitelist 123,456 备注`（也可以在群内回复某人并发送命令）。
- 权限：管理员（群管理员视具体配置）。

/UnWhitelist
/unwhitelist
- 作用：管理员将 TGID 从白名单移除。
- 用法：`/UnWhitelist 123,456`（或回复某人并 `/UnWhitelist`）。
- 权限：管理员。

/License
/license
- 作用：管理员发放短期“注册资格”（类似一次性许可证/资格），并尝试私信目标。
- 用法：`/License 123,456`（或回复某人并 `/License`）。
- 权限：管理员。
- 备注：发放后会尝试私信目标（若目标未私聊机器人，发送可能失败）。

/Remove
/remove
- 作用：管理员或被授权的群管理（取决配置）强制移除/注销账号（属于手动移除，记录审计）。
- 用法：`/Remove 123,456 原因：xxx` 或在群里回复某人并 `/Remove`。
- 权限：管理员或群管理（代码中检查两者之一）。

/Harem
/harem
- 作用：普通用户向指定 TGID 发起“定向邀请”预留注册资格（由邀请方为目标预留注册名额）；私聊触发会显示更详细回执，群内触发仅回执结果并尽量不在群里暴露敏感信息。
- 用法：`/Harem <target_tgid>` 或在群内回复目标并 `/Harem`。
- 权限：注册/已绑定用户可邀请（需满足邀请策略、冷却等）。
- 备注：不会直接把邀请码发到群里；机器人会尝试私信目标并引导其私聊 `/start` 完成注册。

/CutTree
/cuttree
- 作用：管理员或群管理执行“砍树”操作：从指定根 TGID 向下递归移除（注销）整棵邀请树。
- 用法：`/CutTree 123,456 原因：xxx` 或回复某人并 `/CutTree`。
- 权限：管理员或群管理。
- 注意：这是破坏性操作，会注销多个账号，请谨慎使用。

/Eliminate
/eliminate
- 作用：管理员高危命令，销毁某个根及其下属（类似彻底删除后宫，权限更严格，仅管理员可用）。
- 用法：`/Eliminate 123,456 原因：xxx` 或回复某人并 `/Eliminate`。
- 权限：仅管理员。
- 注意：不可逆，操作前请三思并保证参数正确。

/Disband
/disband
- 作用：用户在私聊中解除自己的直接后宫关系（仅向下 1 层），并注销对应账号记录。
- 用法：私聊发送 `/disband` 或 `/disband <tgid...>`（指定 TGID 则只解除对应关系）。
- 权限：私聊用户（仅作用于自己的后宫或指定目标）。
- 备注：`/disband --yes` 用于确认整网解散（如果需要确认）。

/Member
/member
- 作用：管理员添加“群管理”账号（Group Admin）或查询群管理列表（`/Member All`）。
- 用法：`/Member 123,456` 或 `/Member All`。
- 权限：管理员。

/UnMember
/unmember
- 作用：管理员移除群管理。
- 用法：`/UnMember 123,456` 或回复用户并 `/UnMember`。
- 权限：管理员。

其他说明
- 回调按钮（Inline Button）和菜单也被大量使用，很多功能不是通过 Slash 命令而是通过按钮触发（例如用户面板内的按钮、刷新、切库等）。这些按钮的名称和位置可以在 `transport/telegram/router` 目录下查看 `Menus` 定义和各处理函数。
- 有些命令支持在群里回复某人来指定目标（常见于管理员命令），也有的仅支持私聊（例如 `/admin`、用户面板相关命令）。

权限说明
- 管理员：由启动时的 `Options.AdminIDs` 与 `Options.OwnerID` 定义，拥有高级操作权限（创建用户、删除用户、批量续期、发放资格等）。
- 群管理：可在特定命令（如移除、砍树等）中被授予部分管理权限，实际效果依赖数据库中设置（可用 `/Member`、`/UnMember` 管理）。